import http from'node:http';import{readFile,stat}from'node:fs/promises';import{extname,join,resolve}from'node:path';
const root=resolve(process.argv[2]||'.'),port=Number(process.env.PORT||4173),types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.webmanifest':'application/manifest+json'};
http.createServer(async(req,res)=>{try{let pathname=decodeURIComponent(req.url.split('?')[0]);if(pathname.startsWith('/cortez-garage-app/'))pathname=pathname.slice('/cortez-garage-app'.length);let p=join(root,pathname);if((await stat(p)).isDirectory())p=join(p,'index.html');res.setHeader('Content-Type',types[extname(p)]||'application/octet-stream');res.end(await readFile(p))}catch{res.statusCode=404;res.end('Not found')}}).listen(port,'0.0.0.0',()=>console.log(`Cortez Garage: http://localhost:${port}/cortez-garage-app/`));
